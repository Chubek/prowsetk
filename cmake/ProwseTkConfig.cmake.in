@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
if(@PROWSETK_HAVE_OPENSSL@)
    find_dependency(OpenSSL 3.0 COMPONENTS SSL Crypto)
endif()
include("${CMAKE_CURRENT_LIST_DIR}/ProwseTkTargets.cmake")

check_required_components(ProwseTk)
